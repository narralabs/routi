{
  "name": "krog-bot",
  "private": true,
  "version": "0.0.1",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "pnpm --filter krogd dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  }
}
